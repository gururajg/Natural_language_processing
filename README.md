This project cleans texts using stopwords and stemmer, creates bag of words, split dataset for training and testing, and predicts test set using Naive Bayes.

The project was run on Spyder(Python 3.6)
